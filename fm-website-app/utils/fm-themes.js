import { createTheme } from "@nextui-org/react"

const functionalMovementThemes = {
    lightTheme: createTheme({
        type: 'light',
        theme: {
          colors: {
            
          }, // optional
        }
      }),      
      darkTheme: createTheme({
        type: 'dark',
        theme: {
          colors: {}, // optional
        }
      })
}

export default functionalMovementThemes